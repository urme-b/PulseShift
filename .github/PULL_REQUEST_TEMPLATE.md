## Summary

What this change does and why.

## Checklist

- [ ] `make test` passes
- [ ] `ruff check` and `ruff format --check` are clean
- [ ] `mypy research/pulseshift --ignore-missing-imports` is clean
- [ ] If the served model changed: `make model` was run; `model.json` / `model.js` are in sync
- [ ] If an analysis changed: regenerated tables/figures are committed
- [ ] Leak-free, out-of-time discipline preserved

## Notes

Anything reviewers should look at closely.
